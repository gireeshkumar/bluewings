import { GTostringPipe } from './g-tostring.pipe';

describe('GTostringPipe', () => {
  it('create an instance', () => {
    const pipe = new GTostringPipe();
    expect(pipe).toBeTruthy();
  });
});
